version: '3'
services:
  sut:
    image: osseus:latest
    env_file: .env
#     command: ["pytest", "tests/test_server.py"]
    command: ["bash", "tests/test.sh"]
  app:
    image: osseus:latest
    env_file: .env
    ports:
      - "8080:8080"
